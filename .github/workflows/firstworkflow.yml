name: First workflow test by Zain
on:
  [workflow_dispatch,push]
jobs:
  first-job:
        runs-on:ubuntu-latest
        steps:
           - uses: actions/checkout@v4
           - run: cat firstfile.txt
